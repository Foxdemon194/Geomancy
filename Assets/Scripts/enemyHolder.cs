using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class enemyHolder : MonoBehaviour
{
    //public Encounter[] randEncounters;
    private void Start()
    {
        //Encounter[] randEncounters = Resources.LoadAll("Encounter") as Encounter[];
    }

    void Update()
    {
        //GetComponent<encounterDisplay>().encounter = (Encounter)pickedEncounter;
    }
}
